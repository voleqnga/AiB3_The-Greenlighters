// ========================================
// Screen 0: Landing — Chọn vai trò HR / Ứng viên
// ========================================
const Screen0Component = {
    init() {
        if (Config.DEBUG) console.log('[Screen0] Initialized');
    },
    cleanup() {}
};
